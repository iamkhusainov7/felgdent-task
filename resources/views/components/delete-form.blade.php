<form id="{{$formId}}">
    <p>{{$text ?? 'Are you sure you want to delete item?'}}</p>
    <div class="m-t-20 text-left">
        <a href="#" class="btn btn-white" data-dismiss="modal">Close</a>
        <button type="submit" class="btn btn-danger">Yes</button>
    </div>
</form>